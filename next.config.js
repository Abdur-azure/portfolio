/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    /* config options here 
    images: {
        domains: ["cdn.hashnode.com"],
    },
    reactStrictMode: false,*/
}

module.exports = nextConfig
